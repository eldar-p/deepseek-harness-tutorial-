# 018 — Field beta: CLI parseArgs + help coverage

**Этап:** field beta  
**Статус:** ✅ done

## Goal

Export and unit-test `parseArgs`, `cmdHelp`, `cmdPresets`, unknown-command exit code.

## Done

- `parseArgs` exported from `src/cli.js` (deduped `--dry-run`)
- `test/cli.test.js`
