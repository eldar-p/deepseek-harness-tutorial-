name: nightly

on:
  schedule:
    - cron: '17 4 * * *'
  workflow_dispatch:

jobs:
  test-matrix:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm test
      - run: npm run test:coverage
        env:
          GIM_COVERAGE_MIN: '80'
          GIM_COVERAGE_FLOOR: '78'
      - run: npm run audit:prebeta
      - run: npm run audit:security
      - run: npm run test:security
      - name: Honest eval (skip if UI down)
        run: npm run test:honest
        env:
          GIM_UI: http://127.0.0.1:7545
      - name: doctor readiness (field)
        run: node bin/gim.js doctor --readiness --stage=field
      - name: field-lite
        run: npm run field:lite
        env:
          GIM_NO_BANNER: '1'
